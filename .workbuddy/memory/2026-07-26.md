# 2026-07-26

- 生成 `docs/code-index.xml`：扫描全部 117 个 .ts 文件的代码索引，包含关键变量/类/函数定义位置、文件简述、数据流、依赖关系、测试清单，用于节省后续对话 Token。

## v1.2 变体包显示功能实现

- 依据 `docs/第二绿洲_玩法与AI变更清单.xml` 实现 A 优先级玩法机制的显示支持：
  - 类型扩展：`TurnResult` 增加 `RoundIndex` / `FirstPlayerIndex` / `LeaderTax` / `SprintBonus` / `Revenge`；`PlayerSnapshot` 增加 `RevengeToken`；`GameConfig` 增加 4 个机制开关与参数；`DiceMode` 增加 `Revenge`。
  - 核心逻辑：`GameState` 实现轮次计数与顺位轮换、每轮结束公敌税结算、枯竭冲刺奖励、抢夺/崩坏后的复仇令牌发放、`Revenge` 模式掷骰与结算。
  - UI 显示：`GameStageView` 顶部状态栏显示轮次/先手；新增枯竭冲刺横幅；席位 flags 显示复仇令牌；控制台在持有令牌时显示复仇按钮与目标选择器；战局日志记录轮次切换、公敌税、冲刺、复仇事件。
  - 本地对局默认使用 `CreateVariantConfig`（开启公敌税/顺位轮换/枯竭冲刺，关闭复仇突袭）。
- 同步修复 `NetworkGameStore` 实现 `IGameStore` 新接口；更新 11 个测试文件的 mock 数据以匹配新类型。
- 验证：`tsc --noEmit` 零错误；`vitest run` 36 文件 280 测试全过；`vite build` 成功（产物 151.76 kB）。

## v1.2 变体包补充验证与变更报告

- 补充 `src/Core/GameStateVariant.test.ts` 11 个单元测试，覆盖公敌税唯一/并列/默认关闭、顺位轮换、枯竭冲刺正收益/倒扣/默认关闭、标准变体配置。
- 修复 `GameState.AdvanceToNextPlayer` 顺位轮换逻辑：仅当当前阶段为 `SelectMode` 主循环时才递增 `RoundIndex`，避免发射阶段交错推进时错误计轮。
- 生成 `docs/v1.2核心逻辑变更报告.html`：逐项对照变更清单，列出改动位置、原因、预期效果，以及 4 处模糊/冲突条目的处理建议。
- 最终验证：`tsc --noEmit` 零错误；`vitest run` 37 文件 291 测试全过。

## 全量游戏总结（Fluent UI HTML）

- 生成 `docs/第二绿洲_游戏总结_FluentUI.html`（84KB，单文件，19 个章节）：通读全部 117 个 .ts + 计划书/变更清单/平衡报告/code-index，整合为 Fluent 2 设计风格的导航式报告。
- 内容覆盖：项目概览、分层架构、完整规则系统（含 14 项裁定总表 + 数值表）、占领公式、各层真实高亮代码段（Core/AI/Store/Render/UI/Net/Audio/Server/Config/App）、蒙特卡洛平衡数据、v1.2 变体包、测试覆盖（37 文件 291+ 测试）、部署与附录。
- 左侧粘性 Fluent 导航栏 + 19 锚点滚动监听（scroll-spy）。

## 运行目录纠错

- 用户误在 `D:\Project Regolith\` 顶层跑 `npm run preview` 报 ENOENT（找不到 package.json）。实际项目根在 `D:\Project Regolith\Code\`，npm 命令需先 `cd` 到该目录。已在该目录启动 `vite preview`（http://localhost:4173/）。

## 亮色主题严格化改造（白色背景 + 黑色文字）

- 目标：启用亮色主题时，全站 UI 纯白背景、文字全黑、Canvas 也白底。
- 新增 `src/UI/CanvasTheme.ts`：`CanvasPalette()` 每帧读取 `ThemeManagerInstance.Current` 返回 `DARK`/`LIGHT` 调色板（LIGHT 白底 + 深灰/品牌青内容色）；渲染器 rAF 持续运行故切主题自动适配，无需手动刷新。
- 改造 Canvas 渲染器读取调色板：`StarfieldRenderer`（已完成）、`MenuViewportRenderer`（已完成：移除未用 COLORS 导入）、`OasisBoardRenderer`（棋盘文字/当前高亮/格描边/外框改走 `P.BoardText/CurrentHighlight/CellStroke/BoardPublic`；目标色每帧重算以支持主题切换平滑过渡）、`DiceStage`（聚焦光晕改 `P.DiceFocus`，亮色透明）、`ChainBadgeAnimation`（徽章背景亮色转 `rgba(255,255,255,0.92)`）。
- `StyleInjector.ts` 亮色块收尾：残留青色文字（本地玩家名/busy状态/FPS/输入框/设置激活项/作战沙盘标题/设置标题）统一转黑；`.settings-overlay` 与 `.gameover-screen` 暗色遮罩白化；`.settings-section` 改纯白。
- 验证：`tsc --noEmit` 零错误；`npm run build` 成功（175.56 kB）。
- 已为本次修复编写 `CHANGELOG.md` 的 `[1.0.1]` 章节（纯白背景+黑字主题修复，含主题系统/Canvas/UI-CSS/验证四节）。
- 保留的有意语义色（非页面/文字违规）：徽章 ×2/×3/断链状态色、NumberPop 增减绿/红、SeatPulse 边框、GameOver 胜者名/排行榜玩家名阵营色、turn-banner/sprint-banner 阵营/警示色、error 状态红。如用户要求强制纯黑需另行处理。
