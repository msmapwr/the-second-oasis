## 代码检测报告改良

基于 fuck-u-code 检测报告（总分 91.32/100），修复了 Top 10 问题文件中的核心问题：

### 已修复
1. **Synthesizer.ts** — 23处 Web Audio API 无防护问题
   - 新增 `_IsCtxValid()` 检查 AudioContext 状态
   - 所有 `createOscillator`/`createGain`/`createBufferSource` 包在 try-catch 中
   - 重构 `_PlayTone`/`_PlayTriangle` 参数为 `ToneParams` 对象（减少参数数量）
   - 新增 `_CreateOscWithEnvelope` 和 `_CreateScheduledOsc` 复用振荡器创建逻辑

2. **GameStageView.ts** — 构造函数8参数 + 3处 null 守卫
   - 提取 `GameStageViewConfig` 接口，构造函数改为单对象参数
   - `_SprintBanner` 改为 `HTMLElement | null`，`_Refresh` 中添加 null 检查
   - 同步更新 AppController 调用点

3. **AnimationCoordinator.ts** — `_PopNumber`/`_PopPublic` 重复代码
   - 提取 `_PopFromElement(El, Delta)` 通用方法

4. **NumberPopAnimation.ts** — Dispose 加固 + 重复逻辑
   - `Dispose()` 检查 `parentNode` 后再 `remove()`
   - 提取 `_ComputeStyle(T)` 方法供 constructor 复用

5. **MenuViewportRenderer.ts** — Dispose 加固
   - `Canvas.remove()` 前检查 `parentNode`

6. **Dom.ts** — ToggleClass 防护
   - 添加 `!Node || !ClassName` 参数守卫

7. **TestDom.ts** — FakeElement 完善
   - 添加 `parentNode` 属性，`appendChild` 自动设置，`remove` 自动清除

### 未修复（低风险/伪阳性）
- GameState.ts 复杂度（核心逻辑稳定，115测试覆盖）
- MainMenu/AppController 命名（`_前缀`=项目规范，工具误报）
- MonteCarloSimulation（非生产代码，模拟器无需紧绷）
- AnimationCoordinator/ChainBadge 测试（预存 `document is not defined`）
- AI 集成测试偶发超时（预存问题）

### 验证
- tsc --noEmit: ✓ 零错误
- vitest: 341/342 通过（1个预存偶发失败不变）
- vite build: ✓ 89模块，1.58s

---

### 新拟态 UI 重构 — 全部完成

从「像素风+CRT霓虹+赛博骨架」全面重构为「纯粹新拟态（Neumorphism）」。
暗色底色 #202020，亮色底色 #e0e0e0，CSS变量驱动双主题。
按钮：凸起→hover上浮2px→active凹陷。输入框：内阴影凹陷。
圆角：容器8px / 元素4px / 相交边0px。

删除内容：pixel-border, pixel-btn, glow-oasis, glow-alert, crt-overlay, crt-vignette, crt-alert, clip-path 斜切(ignition/link-btn/console-btn/turn-banner/sprint-banner/go-btn/mp-card/mp-form/mp-code-card/settings-gear), 赛博角标(cf-corner/cockpit-panel::before/::after/vp-ring/stage-header::before/::after/player-seat::after/is-current::before/hotseat-card::before/::after/gameover-card::before/::after/settings-header::before), CRT扫描线。

文件变更（24个文件）：
- Theme.ts: COLORS + CSS_VARIABLES 全量重写
- StyleInjector.ts: GLOBAL_CSS 重写（删除~600行像素/CRT代码，全面新拟态化）
- theme.css: @theme变量+utility层重写
- CanvasTheme.ts: DARK/LIGHT双调色板重写
- RenderContext.ts: 删除 imageSmoothingEnabled=false
- DiceStage/OasisBoard/MenuViewport: Canvas新拟态适配
- AppController.ts: 删除 CRT 层创建+控制代码
- 14个组件文件: font-pixel→font-mono/disp, pixel-scroll删除, Tailwind类名替换
- PlayerPalette/ChainBadge/NumberPop/SeatPulse/MainMenu: COLORS引用更新
- 阵营色降饱和度（#2563EB/#7C3AED/#D97706/#DB2777）

验证：
- tsc --noEmit: ✓ 零错误
- npm run build:gh: ✓ 成功（89 modules, 224.93KB, gzip 71.57KB）
- Git: backup/pre-neu-rewrite（备份）+ feat/neumorphism-rewrite（工作分支）
